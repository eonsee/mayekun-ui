# 开发环境配置（vite dev / npm run dev）
# API 基础路径（走 Vite 代理，避免 CORS）
VITE_API_BASE_URL=/admin
# 上传文件访问路径（开发环境走 Vite 代理，保持相对路径）
VITE_UPLOAD_URL=
# Vite 代理目标地址（后端服务地址）
VITE_PROXY_TARGET=http://api.mayekun.com
